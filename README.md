# museums-explorer-backend

## How to run:

- Create `.env` to config DB (`.env.example`)
- Open terminal and run `yarn` to install dependency
- For development, run `yarn dev`
- For production, run `yarn build`, then run `yarn start`